# Backend base URL for production (your Render service URL)
# Leave empty for local dev — Vite proxy forwards /api to localhost:3024
VITE_API_BASE_URL=

# App display name (optional)
VITE_APP_NAME=RecruitAI
